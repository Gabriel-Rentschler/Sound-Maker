// Buffer
    //sf::Image img;
    //img.create(800, 600, sf::Color(0, 0, 0));

    //img.setPixel(400, 300, sf::Color(255, 255, 255));
    //img.setPixel(401, 300, sf::Color(255, 255, 255));

    //sf::Texture texture;
    //texture.loadFromImage(img);

    // Sprite buffer
    //sf::Sprite sprite;
    //sprite.setTexture(texture);

//sprite.move(x * fps, y);

//window.draw(sprite);

//sf::Clock clock;
float lastTime = 0;
float x = 800;
float y;

//float currentTime = clock.restart().asSeconds();
//float fps = 1.f / (currentTime - lastTime);
//lastTime = currentTime;